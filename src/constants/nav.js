// Canonical destinations for cross-page navigation. Import these rather
// than hardcoding paths, so a route change is one edit, not a hunt across
// every page that links back.
export const ADMISSIONS_TAB = '/admin/students?tab=admissions'
export const STUDENTS_TAB = '/admin/students'